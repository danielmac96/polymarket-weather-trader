{
  "name": "@pwa/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "migrate": "echo 'phase 2: drizzle migrate not yet wired'",
    "seed": "echo 'phase 2: seed not yet wired'",
    "generate": "echo 'phase 2: drizzle generate not yet wired'",
    "studio": "echo 'phase 2: drizzle studio not yet wired'"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.5.0"
  }
}
